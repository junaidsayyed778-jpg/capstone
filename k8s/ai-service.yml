apiVersion: v1
kind: Service
metadata:
  name: ai-service
  labels:
    name: ai-service
spec:
  selector:
    app: ai-server
    type: ClusterIP
  ports:
    - port: 80
      name: ai-http
      targetPort: 3000
